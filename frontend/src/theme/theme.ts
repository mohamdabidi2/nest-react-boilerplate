import { primaryColor } from "./colors";

export const customTheme = {
  token: {
    colorPrimary: primaryColor,
    borderRadius: 3,
  },
  components: {
    Button: {
      colorPrimary: primaryColor,
    },
  },
};
